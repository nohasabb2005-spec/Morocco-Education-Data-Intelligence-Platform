name: 'education_platform'
version: '1.0'
config-version: 2

profile: 'education_platform'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"

clean-targets:
  - target
  - dbt_packages

models:
  education_platform:

    silver:
      +schema: silver
      +materialized: table

    gold:
      +schema: gold
      +materialized: table